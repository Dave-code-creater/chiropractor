apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: chiropractor
  labels:
    app: user-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        tier: backend
    spec:
      containers:
      - name: user-service
        image: your-ecr-repo/chiropractor-user-service:latest
        ports:
        - containerPort: 3002
          name: http
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: USER_SERVICE_PORT
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: NODE_ENV
        - name: PRO_POSTGRESQL_USER
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: USER_DB_USER
        - name: PRO_POSTGRESQL_PASS
          valueFrom:
            secretKeyRef:
              name: chiropractor-secrets
              key: USER_DB_PASS
        - name: PRO_POSTGRESQL_HOST
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: USER_DB_HOST
        - name: PRO_POSTGRESQL_PORT
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: USER_DB_PORT
        - name: PRO_POSTGRESQL_NAME
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: USER_DB_NAME
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: chiropractor-secrets
              key: JWT_SECRET
        - name: JWT_EXPIRES_IN
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: JWT_EXPIRES_IN
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: LOG_LEVEL
        - name: RATE_LIMIT_WINDOW_MS
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: RATE_LIMIT_WINDOW_MS
        - name: RATE_LIMIT_MAX_REQUESTS
          valueFrom:
            configMapKeyRef:
              name: chiropractor-config
              key: RATE_LIMIT_MAX_REQUESTS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: chiropractor
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
  - protocol: TCP
    port: 3002
    targetPort: 3002
    name: http
  type: ClusterIP 