{
  "name": "chiropractor-root",
  "private": true,
  "scripts": {
    "migrate": "npm --prefix auth-service run migrate && npm --prefix user-service run migrate && npm --prefix report-service run migrate && npm --prefix appointment-service run migrate && npm --prefix booking-service run migrate"
  }
}
